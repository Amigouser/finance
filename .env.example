# Скопируйте в .env и поменяйте значения
# Минимум 8 символов; используйте openssl rand -hex 24 или pwgen
ACCESS_TOKEN=bbd8a64cfea4e57

# Порт, на котором слушает приложение (за reverse proxy)
PORT=3000
HOST=0.0.0.0

# Путь к файлу SQLite (создастся автоматически)
DB_PATH=./data/finance.db
